# Proyecto Unreal

Coloca aquí el proyecto Unreal (carpeta con el `.uproject` y `Content/`, `Source/`, etc.).

Convención sugerida: `unreal/ProjectName/` para no mezclar metadatos del repo con la raíz del engine.

Cuando exista el proyecto, actualiza `TECH_DECISIONS_LOG.md` con la versión de UE y plataformas.
